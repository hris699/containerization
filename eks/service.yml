apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  annotations: 
    service.beta.kubernetes.io./aws-load-balancer-type: nlb
  namespace: default
  labels:
    app: nodejs-app
spec:
  selector:
    app: nodejs-app
  type: LoadBalancer
  ports:
    # - name: backend
    #   protocol: TCP
    #   port: 8181
    #   targetPort: 8181
    # - name: db
    #   protocol: TCP
    #   port: 27017
    #   targetPort: 27017
    - name: frontend
      protocol: TCP
      port: 5000
      targetPort: 3000
      nodePort: 31271
      